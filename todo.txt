
✅3) Сделать корректное отображение при отсутвтии найденных устройств
✅4) loader корректно настроить уже наконец
5) 
  Future<bool> startScan() async {
    if (_isScanning) return true;

    _isToggleOn = true;

    debugPrint("[reBlue] запрашиваем доступ к разрешениям");
    _isScanning = true;
    notifyListeners();

    final permissionsGranted = await requestBluetoothPermissions();

    if (!permissionsGranted) {
      _isToggleOn = false;
      _isScanning = false;
      notifyListeners();
      return false;
    }
ЕЛСИ ПОЛЬЗОВТЕЛЬ ОТКЛОНИЛ ДВА РАЗА, ВЫДАТЬ СВОЁ КАСТОМНОЕ МЕНЮ С ЗАХОДОМ В НАСТРОЙКИ
И ДОБАВЛЕНИЕМ РАЗРЕШЕНИЯ ВРУЧНУЮ.
6) добавить под капотом auto connect в случае если пользотвель подклчился а ему отказали ( тихо в фоне )